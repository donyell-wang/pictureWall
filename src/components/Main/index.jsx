import React, { Component } from 'react';

class Main extends Component {
  constructor (...arg) {
    super(...arg);
    this.state = {
    };
  }

  render() {
    return (

    );
  }
}

export default Main;
